CREATE TABLE foo(
  id INT NOT NULL,
  name VARCHAR(255) NOT NULL,
  nu NUMBER(255, 2) NOT NULL,
  dd DATE NOT NULL
);

getAll:
SELECT * FROM foo;

new:
INSERT INTO foo VALUES ?;

create:
INSERT INTO foo (id, name, nu, dd) VALUES ?;
